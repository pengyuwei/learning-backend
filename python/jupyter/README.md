# 基础环境

```
% python3 --version
Python 3.7.3

sudo apt install jupyter-core
sudo apt install jupyter-client
sudo apt install jupyter-notebook
pip3 install jupyter
pip3 install jupyter-kernelspec

python -m ipykernel install --user

jupyter-kernelspec list
jupyter notebook
```